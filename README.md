# dive-log

개인 다이빙 로그북 — 단일 `index.html` 페이지 (지도 + 다이브 기록).

## 다이브 추가 방법

`index.html`을 열어 `const DB = {...}` 안의 `dives` 배열에 한 줄 추가한 뒤 커밋 & 푸시:

```bash
git add index.html && git commit -m "add dives" && git push
```

1~2분 뒤 자동 반영됩니다.

## 배포 URL

https://pjhyun7.github.io/dive-log/
